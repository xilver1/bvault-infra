#_preseed_V1
# Observabilty - Debian 13 (trixie)

### Localization
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select es

### Network - static, wired NIC pinned (auto picked wifi and prompted for WEP)
d-i netcfg/choose_interface select enp1s0
d-i netcfg/disable_autoconfig boolean true
d-i netcfg/get_ipaddress string 192.168.0.192
d-i netcfg/get_netmask string 255.255.255.0
d-i netcfg/get_gateway string 192.168.0.1
d-i netcfg/get_nameservers string 192.168.0.1
d-i netcfg/confirm_static boolean true
d-i netcfg/get_hostname string observability
d-i netcfg/get_domain string internal
d-i netcfg/hostname string observability
d-i netcfg/wireless_wep string

### Mirror
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

### Accounts - no root login, one sudo user, key-only
d-i passwd/root-login boolean false
d-i passwd/make-user boolean true
d-i passwd/user-fullname string ansible
d-i passwd/username string ansible
d-i passwd/user-password-crypted password ${ADMIN_PASSWORD_HASH}
d-i passwd/user-default-groups string sudo

### Clock
d-i clock-setup/utc boolean true
d-i time/zone string Europe/Madrid
d-i clock-setup/ntp boolean true

### Partitioning - LVM, everything in one filesystem
d-i partman-auto/method string lvm
d-i partman-auto/disk string /dev/disk/by-id/ata-EMTEC_X250_512GB_TD2547X250F0107
d-i partman-auto-lvm/guided_size string max
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

### Apt
d-i apt-setup/disable-cdrom-entries boolean true
d-i apt-setup/cdrom/set-first boolean false
d-i apt-setup/non-free-firmware boolean true

### Packages - headless only
tasksel tasksel/first multiselect standard, ssh-server
d-i pkgsel/include string curl ca-certificates gnupg sudo python3
d-i pkgsel/upgrade select full-upgrade
popularity-contest popularity-contest/participate boolean false

### Bootloader
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string /dev/disk/by-id/ata-EMTEC_X250_512GB_TD2547X250F0107

### Finish
d-i finish-install/reboot_in_progress note

### late_command
# The whole provisioning body now lives in bootstrap.sh. It is injected into the
# initrd root, so during install it is readable at /bootstrap.sh (initrd = /).
# Copy it into the target and run it chrooted. No backslash-continuation chain.
d-i preseed/late_command string \
    cp /bootstrap.sh /target/tmp/bootstrap.sh; \
    in-target sh -c 'chmod +x /tmp/bootstrap.sh && /tmp/bootstrap.sh'; \
    in-target rm -f /tmp/bootstrap.sh
